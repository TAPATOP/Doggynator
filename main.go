package main

import "fmt"

func main() {
	questions := []string{"Does it weigh more than 20 kilograms?", "Does it have \"glued\" ears?", "Are its legs disproportionate compared to its boddy"}
	fmt.Println(questions[0])
}
